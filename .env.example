# thepdf.ink build-time configuration.
# Copy to `.env` and fill in before running `npm run build` / `npm run deploy`.

# Hosted checkout links per one-time tier.
# Paste your Gumroad / Lemon Squeezy / Ko-fi / Stripe Payment Link for each.
# When unset, the buy buttons fall back to the /pricing page.
VITE_CHECKOUT_URL_PERSONAL=
VITE_CHECKOUT_URL_PRO=
VITE_CHECKOUT_URL_BUSINESS=

# Back-compat alias used for Personal if VITE_CHECKOUT_URL_PERSONAL is unset.
VITE_CHECKOUT_URL=

# Google AdSense publisher ID, e.g. ca-pub-1234567890123456.
# Leave empty to keep ads off in the React app. (The static content pages in
# /public also carry the AdSense snippet — replace ca-pub-XXXXXXXXXXXXXXXX there.)
VITE_ADSENSE_CLIENT=
